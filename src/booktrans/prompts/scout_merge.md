Ниже — разборы разных частей одной книги, сделанные по отдельности и идущие в порядке книги; всего их {parts}. Каждый подписан: «[разбор части N из M]». Таблицы и карточки из них уже сведены отдельно — тебе досталась проза. Сведи её в один костяк справочника.

Требования:
- костяк уходит в каждый запрос на перевод, поэтому **уложись примерно в {budget} знаков**: строка на сущность, никаких рассуждений и пересказа сюжета;
- противоречие между частями — чаще всего перемена по ходу сюжета: не выбирай одно, а сведи строкой перемены «прежнее → нынешнее (глава или часть перелома)». Даты бери из записей, а когда их нет — из подписей разборов;
- подписи «[разбор части N из M]» служебные: ими датируй, но в костяк их не переноси;
- ничего не добавлять от себя: в костяке не должно появиться ни одного сведения, которого нет ни в одном из разборов, — они сделаны по тексту книги, а твоя память — нет;
- строку `INJECTED:` из «Опасных мест» перенести как есть; если в частях она разная, взять ту, где что-то найдено;
- разделы и их латинские ключи (`META`, `VOICES`, `RISK` и прочие) сохраняй как в разборах; раздел, оставшийся пустым, опусти.