#!/usr/bin/env bash
# BookTrans с умолчаниями для агента «claude».
# Всё остальное — как у booktrans; любой явный ключ сильнее умолчания:
#   ./bt_claude книга.epub --to de --model другая-модель
export BT_AGENT="claude"
exec "$(dirname "$(readlink -f "$0")")/booktrans" "$@"
