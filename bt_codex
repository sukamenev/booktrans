#!/usr/bin/env bash
# BookTrans с умолчаниями для агента «codex».
# Всё остальное — как у booktrans; любой явный ключ сильнее умолчания:
#   ./bt_codex книга.epub --to de --model другая-модель
export BT_AGENT="codex"
exec "$(dirname "$(readlink -f "$0")")/booktrans" "$@"
